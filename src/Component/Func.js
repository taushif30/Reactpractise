import React from 'react'

function Func(props) {
  return (
    <div>
      <h1>{props.send.arr.Name}</h1>
      <h1>{props.send.arr.age}</h1>
      <h1>{props.send.arr.course}</h1>
        
      
    </div>
  )
}

export default Func
